# --- Core API (required to run the server) ---
# Floors chosen so prebuilt wheels exist for Python 3.13 (no local compiler needed).
fastapi>=0.115.0
uvicorn>=0.30.0
sqlalchemy>=2.0.36
python-dotenv>=1.0.1
pydantic>=2.9.0
python-multipart>=0.0.9
pillow>=11.0.0
numpy>=2.1.0

# --- Gemini: AI advisory endpoints + vision-based invoice OCR ---
google-genai>=1.0.0

# --- OCR architecture note ---
# OCR now runs ON-DEVICE via ML Kit (app/src/ocr/mlkit.ts).
# The backend receives plain text (ocrText) and only does field extraction.
# PaddleOCR is NOT used. Gemini vision is the fallback when ocrText is absent.
# (paddlepaddle / paddleocr / opencv-python are not needed)
